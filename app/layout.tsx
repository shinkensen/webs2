import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Govind Nair - Developer Portfolio",
  description: "14-year-old programmer specializing in Web Development, DSA, and building innovative solutions. Portfolio showcasing projects and skills.",
  keywords: ["Govind Nair", "Developer", "Web Development", "React", "Next.js", "TypeScript", "Portfolio"],
  authors: [{ name: "Govind Nair" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-black`}
      >
        {children}
      </body>
    </html>
  );
}
